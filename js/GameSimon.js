export class GameSimon {
    constructor(){
        Colors = []
    }

    static addColors = (id, colorOn, colorOff) => {
        Colors.push({
            id: id,
            colorOn: Coloroff,
            colorOff: colorOff,
        })
    }
}